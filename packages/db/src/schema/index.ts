// Schema Drizzle do Bella OS. Vazio no bootstrap (M0); M1 adiciona tenants, identidade e plataforma.
// Regra: toda tabela de negócio tem tenant_id NOT NULL (ADR-004). Ver docs/DOMAIN_MODEL.md.
export {};
