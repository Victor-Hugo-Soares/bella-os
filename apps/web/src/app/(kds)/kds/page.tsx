/**
 * Reservado desde o M4 (ADR-014) — conteúdo real (fila de tickets por estação) é da
 * Fase C (M9). Existe aqui só para provar a estrutura de rotas por superfície.
 */
export default function KdsPlaceholderPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-background px-4">
      <p className="text-sm text-muted-foreground">Cozinha/KDS — chega na Fase C.</p>
    </main>
  );
}
